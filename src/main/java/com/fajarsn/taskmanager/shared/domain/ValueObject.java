package com.fajarsn.taskmanager.shared.domain;

/**
 * Marker interface for value objects in DDD.
 * Value objects are immutable and equality is based on their attributes.
 */
public interface ValueObject {
    // Marker interface - implementations should ovverride equals() and hashCode()
}
